name: CI

on:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - run: "ls /home/runner/work/cyclofix-website/cyclofix-website"
    - uses: srt32/hugo-action@master

